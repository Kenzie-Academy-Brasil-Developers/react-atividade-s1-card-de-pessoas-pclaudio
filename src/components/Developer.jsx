const Developer = ({ name, age, country }) => {
  return (
    <div>
      <h1>Dev: {name}</h1>
      <h3>Idade: {age}</h3>
      <h3>País: {country}</h3>
    </div>
  );
};

export default Developer;
